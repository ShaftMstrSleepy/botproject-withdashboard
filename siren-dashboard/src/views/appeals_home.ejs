<div style="display:flex;flex-direction:column;align-items:center;height:calc(100vh - 60px);justify-content:center;">
  <h2>Select a Server to Appeal</h2>
  <form method="get">
    <select name="guildId" onchange="if(this.value) location='/appeals/'+this.value+'/new';">
      <option value="" disabled selected>Choose a server…</option>
      <% guilds.forEach(g => { %>
        <option value="<%= g.id %>"><%= g.name %></option>
      <% }); %>
    </select>
  </form>
</div>
